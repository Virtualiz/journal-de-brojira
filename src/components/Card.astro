---
const { title } = Astro.props;
---

<div class="card card-border border-accent bg-base-100">
  <div class="card-body">
    <h2 class="card-title">{title}</h2>
    <slot />
        <div class="card-actions justify-end">
      <slot name="cta" />
    </div>
  </div>
</div>
